import requests
import json
import collections
import os

def getBangumiFeed (range):
    requestURL = requests.get("https://bangumi.bilibili.com/api/timeline_v2_global")
    json_data = json.loads(requestURL)
    
    for 
    serverResponse = requestURL.json()
    
    return [serverResponse]
    
def getBangumiDetail ():
    
